package ru.job4j.max;

public class Max {
    public int max(int first, int second);
    //return first + second;
    int resultMax , k;
    k = 1;
    resultMax = k > 0 ? int first + int second : 0;
    return resultMax;
}
